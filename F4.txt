It is important to note that changing a secret will affect all workflows or actions that use that secret. If there are any workflows or actions that depend on the old value of the secret, the user will need to update them accordingly. Additionally, changing a secret will not affect any workflows or actions that are currently running or queued, so the user may need to cancel and re-run them if they depend on the updated secret.

Finally, if a user no longer needs a secret, they can delete it by clicking on the "Delete" button next to it in the "Secrets" page. It is important to be careful when deleting secrets, as this will also affect any workflows or actions that depend on the secre
